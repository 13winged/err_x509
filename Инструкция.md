err_x509 - Средство обхода SSL-сертификата

Простая, безопасная и надежная утилита для автоматического добавления "skip-cert-verify: true" ко всем записям прокси-сервера в файлах конфигурации YAML. Идеально подходит для устранения проблем с проверкой сертификатов SSL/TLS в Clash, NekoBox, V2RayXS и других прокси-клиентах.

- ✅ **TLS Safe**: Сохраняет все параметры TLS/SSL (sni, alpn, отпечатки пальцев и т.д.).
- ✅ **Интеллектуальная обработка**: Работает как с компактными форматами, так и с многострочными форматами
- ✅ **Создание резервной копии**: Автоматически создает резервную копию исходного файла
- ✅ **Статистика**: Показывает подробную статистику обработки
- ✅ **Отсутствие искажения формата**: Поддерживает исходную структуру YAML
- ✅ **Просто**: Один исполняемый файл, установка не требуется
- ✅ **Быстрый**: Обрабатывает даже большие конфигурации за миллисекунды

## 🚀 Быстрый запуск

### Для пользователей Windows
1. **Загрузите** `err_x509.exe` из [Releases](https://github.com/13winged/err_x509/releases)
2. **Создайте ** файл с именем `x509_no_fix.yaml` в той же папке
3. **Скопируйте ** в него вашу конфигурацию YAML.
4. **Запустите ** `err_x509.exe` (дважды щелкните)
5. **Используйте ** сгенерированный `x509_fixed.yaml`

## 📝 Пример использования

### Ввод (`x509_no_fix.yaml`):
``
порт yaml: 7890
прокси-серверы:
  - { имя: Server1, тип: троян, сервер: s1.com, порт: 443, пароль: pass1, sni: s1.com, alpn: ["h2"] }
  - { имя: Server2, тип: vmess, сервер: s2.com, порт: 443, uuid: xxxxx, tls: true }
Выходные данные (x509_fixed.yaml):

порт yaml: 7890
прокси-серверы:
  - { имя: Server1, тип: trojan, сервер: s1.com, порт: 443, пароль: pass1, sni: s1.com, alpn: ["h2"], skip-cert-verify: true }
  - { имя: Server2, тип: vmess, сервер: s2.com, порт: 443, uuid: xxxxx, tls: true, пропуск проверки сертификата: true }

🛠 Расширенное использование
Сборка на основе исходного кода
bash
# Клонировать репозиторий
git clone https://github.com/13winged/err_x509.git
cd err_x509

# Сборка для Windows
go build -o err_x509.exe main.go

# Сборка для Linux
GOOS=linux GOARCH=amd64 go build -o ошибка_x509_linux main.go

# Сборка для macOS
GOOS=darwin GOARCH=amd64 перейти к сборке -o err_x509_mac main.go

Пакетная обработка
Создать process.bat:

batch
@echo off
for %%f in (*.yaml) do (
    if not "%%f"=="x509_fixed.yaml" (
        copy "%%f" "x509_no_fix.yaml"
        err_x509.exe
        move "x509_fixed.yaml" "%%~nf_fixed.yaml"
    )
)
🔧 Технические подробности
Для чего это нужно?
Добавляет параметр skip-cert-verify: true ко всем записям прокси-сервера, в то время как:
Сохранение существующих параметров TLS
Поддержание структуры YAML
Создание резервных копий
Отображение статистики обработки

Поддерживаемые типы прокси-серверов
✅ Trojan (со всеми опциями TLS)
✅ VMess (с настройками TLS/WS)
✅ Shadowsocks
✅ SOCKS5/HTTP
✅ Любой формат прокси-сервера YAML

❓ Часто задаваемые вопросы
Вопрос: Изменяет ли это мой исходный файл?
Ответ: Нет! Создается новый файл x509_fixed.yaml. Исходный файл хранится в резервной копии как x509_no_fix.yaml.backup.

Вопрос: Что делать, если у прокси-сервера уже есть функция skip-cert-verify?
О: Она пропускает его и отображается в статистике. Нет повторяющихся записей.

Вопрос: Безопасно ли это?
О: Безусловно. Это добавляет только один параметр, не удаляет и не изменяет существующие.

В: Что делать, если у меня более 100 прокси-серверов?
О: Работает отлично. Быстро обрабатывает все прокси.
